{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Generate local self-signed cert",
      "type": "shell",
      "command": "./scripts/generate-local-self-signed-cert.sh",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}